#include <iostream>
using namespace std;
int main(){
	int age;
	cout << "Enter your age :";
	cin >> age;
	if (age >= 18){
		cout << " \n You are eligible to vote.";
	}
	else { cout << " \n You are not eligible for voting.";
	cout << "Wait for\t" << 18 - age << "year(s)";
	}
	return 0;
}
